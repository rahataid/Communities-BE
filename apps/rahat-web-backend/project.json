{
  "name": "rahat-web-backend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/rahat-web-backend/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/rahat-web-backend",
        "main": "apps/rahat-web-backend/src/main.ts",
        "tsConfig": "apps/rahat-web-backend/tsconfig.app.json",
        "assets": [
          "apps/rahat-web-backend/src/assets"
        ],
        "webpackConfig": "apps/rahat-web-backend/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "rahat-web-backend:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "rahat-web-backend:build:development"
        },
        "production": {
          "buildTarget": "rahat-web-backend:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "apps/rahat-web-backend/jest.config.ts"
      }
    }
  },
  "tags": []
}
